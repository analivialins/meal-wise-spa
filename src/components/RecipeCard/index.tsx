import { Menus } from "../../interfaces/menus";

type MenuProp = {
    menu: Menus
}

export default function RecipeCard({menu}: MenuProp) {

    console.log(menu);
    
    return(
        <></>
    )
}